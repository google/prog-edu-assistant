# submission.py: A fixed file name where the student's solution
# code is extracted. Typically it will contain a single function
# or a single class definition. For some assignments it may be
# a complete standalone .py file.

def infloop():
    x = 1
    # Loops forever.
    while True:
        x = x+1
    return x
